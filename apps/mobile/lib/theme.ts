/** Product palette for Expo. Prefer these over hardcoded Aidex hexes.
 * System chrome (nav bars, fills, labels) stays in `./native` via PlatformColor. */
export { botColors, tokens } from "@rakazo/ui-tokens";
