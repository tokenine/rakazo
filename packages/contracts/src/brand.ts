/**
 * Single source for user-visible branding.
 *
 * Renaming the product: change BRAND_NAME here, run `scripts/rebrand-display.sh`
 * for the remaining literal copy (i18n catalogs, desktop setup, index.html, docs),
 * then swap logo assets in `packages/ui-tokens/assets/` and `apps/www/public/brand/`.
 * Technical identifiers (`@rakazo/*` scope, `RAKAZO_*` env vars, image names,
 * `dev.rakazo.desktop`, compose project names) are deliberately NOT rebranded —
 * see REBRAND-NOTES.md.
 */
export const BRAND_NAME = "Rakazo";
